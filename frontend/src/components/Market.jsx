// ========================================================== MARKET CHART: stored PostgreSQL market data → DRF → Chart.js. ==========================================================
import {useState} from 'react';import {Line} from 'react-chartjs-2';import {Chart as ChartJS,CategoryScale,LinearScale,PointElement,LineElement,Tooltip,Legend} from 'chart.js';import {get} from '../api.js';ChartJS.register(CategoryScale,LinearScale,PointElement,LineElement,Tooltip,Legend);export default function Market(){const[symbol,setSymbol]=useState('AAPL'),[rows,setRows]=useState([]),[error,setError]=useState('');async function load(e){e.preventDefault();try{setError('');setRows(await get(`/market/latest/?symbol=${symbol}&limit=90`))}catch(x){setError(x.message)}}const data={labels:rows.map(x=>x.date),datasets:[{label:`${symbol} close`,data:rows.map(x=>Number(x.close_price)),tension:.2}]};return <div className="card shadow-sm"><div className="card-body"><h4>Market Data</h4><form className="input-group mb-3" onSubmit={load}><input className="form-control" value={symbol} onChange={e=>setSymbol(e.target.value.toUpperCase())}/><button className="btn btn-primary">Load</button></form>{error&&<div className="alert alert-danger">{error}</div>}{rows.length?<Line data={data}/>:<p>Import data in Django first.</p>}</div></div>}
